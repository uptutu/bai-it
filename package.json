{
  "name": "openen",
  "private": true,
  "scripts": {
    "dev": "node build.mjs --watch",
    "build": "node build.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "esbuild": "^0.25",
    "fake-indexeddb": "^6.2.5",
    "puppeteer": "^24.37.5",
    "typescript": "^5.7",
    "vitest": "^3"
  }
}
